{
  "name": "fullstack",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "npx concurrently 'npm:dev:*'",
    "dev:api": "cd api && bun run start",
    "dev:frontend": "cd frontend && bun run dev"
  },
  "devDependencies": {
    "bun-types": "latest",
    "concurrently": "^8.2.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "workspaces": {
    "packages": [
      "api",
      "frontend"
    ]
  }
}